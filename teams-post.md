# Teams 投稿用の紹介文

そのままコピーして使えます。詳細は README に任せ、投稿では
**「SPO のリストやファイルは Power Automate 以外からも触れる」**という一点に絞り、
なぜ Graph API ではなく REST API なのか、なぜブックマークレットなのかを添えています。

---

## 標準版

**SharePoint のリストやファイルは、Power Automate 以外からも触れます** 🔧

SPO のデータを扱おうとすると、まず Power Automate が候補に挙がると思います。
ただ、その裏側で動いている REST API は、**ブラウザから直接叩けます**。
「リストの中身をざっと確認したい」「ライブラリに置いた JSON を見たい」程度なら、
フローを1本組むより速いです。

それを体験してもらうためのツールを作りました。

### なぜ Graph API ではなく REST API なのか

Graph API を使うにはアプリ登録が必要で、**申請を通さないと試せません**。
ちょっと動きを確認したいだけなのに、そこで止まってしまいます。

一方 SharePoint の REST API（`/_api/web/lists/...` のようなエンドポイント）は、
**ログイン済みのブラウザからならそのまま呼べます**。申請もアプリ登録も不要です。
まず手を動かして感触を掴むには、こちらのほうが圧倒的に早いです。

### なぜブックマークレットなのか

**認証をブラウザ任せにするためです。**

REST API はログイン中のセッションで通ります。つまり
**SharePoint のページ上でコードを動かせば、認証が自動で乗ります**。
逆に、手元のスクリプトや外部ツールから叩こうとすると、
アクセストークンの取得が必要になり、結局アプリ登録の話に戻ってしまいます。

ブックマークレットなら、開いているページの中でコードを実行できるので、この問題を丸ごと回避できます。

### ブックマークレットとは

ブックマークの URL 欄に、ページのアドレスの代わりに JavaScript を書いたものです。
クリックすると、**そのとき開いているページの上でスクリプトが実行されます**。
ページ遷移はしません。

インストールも拡張機能も不要で、やめたくなったらブックマークを消すだけです。
今回のものは、押すと SharePoint のページの右上に小さな操作パネルを出します。

### できること

- リストの新規作成 / アイテム追加 / 一覧表示
- ドキュメントライブラリの指定フォルダのファイル一覧
- ライブラリ上の JSON ファイルの読み込み

叩いているのは普通の REST エンドポイントなので、
**「SPO を API で操作する」入り口としてそのまま応用が利きます。**
本格的に組むときは Graph API やアプリ登録に進むことになりますが、
その前に「何ができるか」を掴むところまでは、これで足ります。

Power Automate を置き換えるものではありません。使い分けはこんなイメージです。

- **定期実行・承認・他サービス連携が要る** → Power Automate
- **今すぐ中身を確認したい / 一度きりの一括処理 / API の挙動を試したい** → 直接 API

### 注意

自分のログインセッションをそのまま使う仕組みなので、
**自分の権限で見えるものしか見えません**。アクセスは通常の操作と同じく監査ログに残ります。

⚠️ **リスト作成とアイテム追加は本当に作られます。まず検証用サイトで試してください。**
削除する機能はあえて入れていません。

導入手順とコードの解説は README にあります。
https://github.com/trie0000/spo-explorer

---

## 短縮版（チャット向け）

SPO のリストやファイルって、Power Automate を使わなくても触れます 🔧

Graph API はアプリ登録の申請が要るので気軽に試せませんが、
SharePoint の REST API なら**ログイン済みのブラウザからそのまま呼べます**。申請も登録も不要です。

体験用にブックマークレットを作りました。
ブックマークレットは、URL 欄に JavaScript を書いたブックマークのことで、
押すと**そのとき開いているページの上でスクリプトが動きます**（インストール不要）。
この方式にしたのは、**SharePoint のページ上で動かせば認証がそのまま乗るから**です。
外部のツールから叩こうとすると、トークン取得＝アプリ登録の話に戻ってしまいます。

ボタン1回で、リストの作成・アイテム追加・一覧表示、
ドキュメントライブラリのファイル一覧、JSON の読み込みが試せます。

自分の権限の範囲でしか動きませんが、**作成系は本当に作られる**ので、まず検証用サイトでどうぞ。
https://github.com/trie0000/spo-explorer

---

## 投稿時のメモ

- リンクを貼ると Teams がカードを展開するので、**URL は本文の最後に置く**と読みやすいです
- 「Power Automate を否定している」と読まれないよう、使い分けの2行は残しておくのが無難です
- Graph API の申請フローそのものへの不満に話が逸れやすいので、
  **「本格的に組むときは Graph に進む」の一文は削らない**ほうが穏当です
- ブックマークレットに馴染みがない人が一定数いる前提で、説明の段落は残しておいてください
- 反応が薄いときは「触ってみたい人向けに 15 分の画面共有します」を追記すると動きやすいです
- 社内の SharePoint 環境の話（サイト名・ライブラリ名など）は投稿にもリポジトリにも書かないでください。
  このツールは環境に依存しない作りにしてあります
