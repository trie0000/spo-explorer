# Teams 投稿用の紹介文

そのままコピーして使えます。詳細は README に任せ、投稿では「何ができるか」と
「試す前の注意」だけに絞っています。

---

## 標準版

**SharePoint の REST API をブラウザだけで試せるツールを作りました** 🔧

ブックマークバーのボタンを1回押すと、開いている SharePoint サイトに小さなパネルが出ます。
アプリ登録も追加の認証も不要で、次のことが試せます。

- リストの新規作成 / アイテム追加 / 一覧表示
- ドキュメントライブラリの指定フォルダのファイル一覧
- ライブラリ上の JSON ファイルの読み込み

自分のログインセッションをそのまま使う仕組みなので、**自分の権限で見えるものしか見えません**。
アクセスは通常の操作と同じように監査ログに残ります。

⚠️ **リスト作成とアイテム追加は本当に作られます。まず検証用サイトで試してください。**
削除する機能はあえて入れていません。

導入手順とコードの解説は README にあります。
https://github.com/trie0000/spo-explorer

「SharePoint のデータを API から触ってみたい」「`$select` ってどう書くんだっけ」
くらいの入り口に使ってもらえればと思います。質問はこのスレッドへどうぞ。

---

## 短縮版（チャット向け）

SharePoint の REST API をブラウザだけで試せるツールを作りました 🔧
ブックマークのボタン1回で、リストの作成・アイテム追加・一覧表示、
ドキュメントライブラリのファイル一覧、JSON の読み込みが試せます。
アプリ登録も追加の認証も不要です。

自分の権限の範囲でしか動きませんが、**作成系は本当に作られる**ので、
まず検証用サイトでどうぞ。手順は README に書いてあります。
https://github.com/trie0000/spo-explorer

---

## 投稿時のメモ

- リンクを貼ると Teams がカードを展開するので、**URL は本文の最後に置く**と読みやすいです
- 反応が薄いときは「触ってみたい人向けに 15 分の画面共有します」を追記すると動きやすいです
- 社内の SharePoint 環境の話（サイト名・ライブラリ名など）は投稿にもリポジトリにも書かないでください。
  このツールは環境に依存しない作りにしてあります
