# Teams 投稿用の紹介文

そのままコピーして使えます。詳細は README に任せ、投稿では
**「SPO のリストやファイルは Power Automate 以外からも触れる」**という一点に絞っています。

---

## 標準版

**SharePoint のリストやファイルは、Power Automate 以外からも触れます** 🔧

SPO の裏側では REST API が動いていて、これは**ログイン済みのブラウザからそのまま呼べます**。
「リストの中身を確認したい」「ライブラリの JSON を見たい」程度なら、フローを組むより速いです。
体験用のツールを作りました。

**なぜ Graph API ではないのか**
アプリ登録の申請が要るので、気軽に試せません。REST API なら申請も登録も不要です。
本格的に組むときは Graph に進めばよく、その手前の「何ができるか」を掴むまではこれで足ります。

**なぜブックマークレットなのか**
認証をブラウザ任せにするためです。SharePoint のページ上でコードを動かせば、
ログイン中のセッションがそのまま効きます。
外部ツールから叩こうとすると、トークン取得＝アプリ登録に戻ってしまいます。

ブックマークレットは、URL 欄に JavaScript を書いたブックマークです。
押すと開いているページの上でスクリプトが動きます
（ページ遷移なし・インストール不要・やめるときはブックマークを消すだけ）。

**できること**
- リストの作成 / アイテム追加 / 一覧表示
- ドキュメントライブラリのファイル一覧
- ライブラリ上の JSON の読み込み

自分の権限で見えるものしか見えず、アクセスは通常どおり監査ログに残ります。
⚠️ **作成系は本当に作られるので、まず検証用サイトで試してください。** 削除機能は入れていません。

https://github.com/trie0000/spo-explorer

---

## 短縮版（チャット向け）

SPO のリストやファイルは、Power Automate を使わなくても触れます 🔧
Graph API はアプリ登録の申請が要りますが、SharePoint の REST API なら
**ログイン済みのブラウザからそのまま呼べます**。

体験用にブックマークレット（URL 欄に JavaScript を書いたブックマーク。
押すと開いているページ上でスクリプトが動きます）を作りました。
この方式なら認証がブラウザ任せで済みます。
ボタン1回で、リストの作成・アイテム追加・一覧表示、ファイル一覧、JSON の読み込みが試せます。

**作成系は本当に作られる**ので、まず検証用サイトでどうぞ。
https://github.com/trie0000/spo-explorer

---

## 投稿時のメモ

- **URL は本文の最後に置く**（Teams がカードを展開するため）
- 「Power Automate を否定している」と読まれないよう、Graph に進む前提の一文は削らない
- ブックマークレットに馴染みがない人向けの説明も残す（消し方まで書くと抵抗が下がります）
- 反応が薄いときは「触ってみたい人向けに 15 分の画面共有します」を追記
- 社内の SharePoint 環境の話（サイト名・ライブラリ名など）は投稿にもリポジトリにも書かない
