# Teams 投稿用の紹介文

そのままコピーして使えます。詳細は README に任せ、投稿では
**「SPO は Power Automate 以外からも触れる／申請なしで試せる／AI に書かせれば手軽」**
という流れに絞っています。

---

## 標準版

**SharePoint のリストやファイルは、Power Automate 以外からも参照・更新できます** 🔧

ただ、それをプログラムで自動化しようとすると、代表的には Graph API を使うか、
MSAL で認証してから REST API を叩くことになります。
**どちらもアプリ登録の申請が要るので、手軽に試せません。**

そこで、**認証済みのブラウザ画面から REST API を呼び出す**ツールにしました。
ブックマークレットなので、事前の申請は要りません。

ブックマークレットは、URL 欄に JavaScript を書いたブックマークです。
押すと開いているページの上でスクリプトが動きます
（ページ遷移なし・インストール不要・やめるときはブックマークを消すだけ）。
SharePoint のページで押せば、ログイン中のセッションがそのまま効きます。

もうひとつの利点は、**Power Automate のように手でフローを組み立てなくていい**ことです。
やりたいことを AI に投げてコードを書いてもらえば、それがそのまま動きます。

**できること**（例としてこのツールに入れてあるもの）
- リストの作成 / アイテム追加 / 一覧表示
- ドキュメントライブラリのファイル一覧
- ライブラリ上の JSON の読み込み

自分の権限で見えるものしか見えず、アクセスは通常どおり監査ログに残ります。
⚠️ **作成系は本当に作られるので、まず検証用サイトで試してください。** 削除機能は入れていません。

**添付の zip を展開して、README から読んでください。** 導入手順とコードの解説が入っています。

---

## 短縮版（チャット向け）

SPO のリストやファイルは、Power Automate 以外からも参照・更新できます 🔧
ただプログラムから自動化しようとすると、Graph API か、MSAL 認証のうえで REST API を叩く形になり、
**どちらも申請が要るので手軽に試せません。**

そこで、認証済みのブラウザ画面から REST API を呼ぶブックマークレットを作りました。
（URL 欄に JavaScript を書いたブックマークで、押すと開いているページ上でスクリプトが動きます）
事前の申請は不要で、フローを手で組み立てる必要もないので、
やりたいことを AI に書いてもらえばそのまま動きます。

**作成系は本当に作られる**ので、まず検証用サイトでどうぞ。
使い方は添付の zip の README にあります。

---

## 投稿時のメモ

- zip を投稿に添付し、**まず README を読んでもらう**流れにする
- 「Power Automate を否定している」と読まれやすいので、
  申請が要る場面では Graph に進む前提であることを口頭で補足できるようにしておく
- ブックマークレットに馴染みがない人向けの説明は残す（消し方まで書くと抵抗が下がります）
- 反応が薄いときは「触ってみたい人向けに 15 分の画面共有します」を追記
- 社内の SharePoint 環境の話（サイト名・ライブラリ名など）は投稿にも配布物にも書かない
